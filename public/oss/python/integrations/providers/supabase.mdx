---
title: "Supabase (Postgres) integrations"
description: "Integrate with Supabase (Postgres) using LangChain Python."
---

[Supabase](https://supabase.com/docs) 是一个开源的 `Firebase` 替代方案。

`Supabase` 构建于 `PostgreSQL` 之上，它提供了强大的 `SQL` 查询能力，并与现有工具和框架实现简单的对接。

> [PostgreSQL](https://en.wikipedia.org/wiki/PostgreSQL) 也被称为 `Postgres`，
> 是一个免费且开源的关系型数据库管理系统 (RDBMS)，
> 强调可扩展性和 `SQL` 兼容性。

## Installation and setup

We need to install `supabase` python package.

<CodeGroup>
```bash pip
pip install supabase
```

```bash uv
uv add supabase
```
</CodeGroup>

## Vector store

See a [usage example](/oss/python/integrations/vectorstores/supabase).

```python
from langchain_community.vectorstores import SupabaseVectorStore
```

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss\python\integrations\providers\supabase.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
