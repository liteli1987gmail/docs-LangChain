---
title: 环境变量
sidebarTitle: Agent Server 环境变量
---
Agent Server 支持特定的环境变量来配置部署。

## `BG_JOB_ISOLATED_LOOPS`

将 `BG_JOB_ISOLATED_LOOPS` 设置为 `True` 以在与服务 API 事件循环不同的事件循环中执行后台运行。

<Warning>
启用此标志不会消除根本问题。它将同步阻塞工作从服务 API 的事件循环移开，以便健康检查停止失败，但阻塞代码继续在后台循环上运行，**将会**继续在生产中造成问题，如吞吐量下降、尾延迟峰值、worker 饥饿或连接池耗尽（见下面的池大小警告），以及负载下的不良扩展。

要正确解决这些问题，请在您的代理中始终使用原生异步驱动和异步代码。这意味着使用异步 HTTP 客户端如 `httpx` 或 `aiohttp`（尽管我们建议缓存客户端以避免加载 SSL 上下文时 CPU 开销）、异步数据库驱动如 `asyncpg` 或 `psycopg[async]`，以及异步模型 SDK。对于不可避免的同步库，将特定调用包装在 `asyncio.to_thread(...)` 或 `loop.run_in_executor(...)` 中，而不是为整个部署启用此标志。
</Warning>

如果图/节点的实现包含同步代码，则应将此环境变量设置为 `True`。在这种情况下，同步代码将阻塞服务 API 事件循环，这可能导致 API 不可用。API 不可用的症状是由于健康检查失败导致应用程序持续重启。

<Warning>
当 `BG_JOB_ISOLATED_LOOPS` 被启用时，每个后台 worker 在自己的线程中运行，并拥有**独立的 Postgres 连接池**。每个 worker 的池大小为 `LANGGRAPH_POSTGRES_POOL_MAX_SIZE // N_JOBS_PER_WORKER`。例如，当 `LANGGRAPH_POSTGRES_POOL_MAX_SIZE=20` 和 `N_JOBS_PER_WORKER=15` 时，每个 worker 只能获得 1 个连接的池。小的每 worker 池更容易受到连接失败的影响，因为单个陈旧连接代表池的很大一部分。如果您启用隔离循环，请确保 `LANGGRAPH_POSTGRES_POOL_MAX_SIZE` 足够大，以便为每个 worker 提供至少几个连接。
</Warning>

默认为 `False`。

## `BG_JOB_MAX_RETRIES`

后台运行在遇到可重试失败（如临时数据库错误、服务器关闭取消）后重试的最大次数。当运行以可重试错误失败时，它会被放回队列并从最后一个检查点步骤恢复。如果运行超过最大重试次数，它将被标记为失败。

默认为 `3`。

## `BG_JOB_SHUTDOWN_GRACE_PERIOD_SECS`

指定服务器在队列收到关闭信号后等待后台作业完成的秒数。之后，服务器将强制终止。默认为 `180` 秒。最大值为 `3600` 秒。设置此值以确保作业在关闭期间有足够的时间正常完成。在 `langgraph-api==0.2.16` 中添加。

## `BG_JOB_TIMEOUT_SECS`

后台运行的超时时间可以增加。但是，Cloud 部署的基础设施对 API 请求强制执行 1 小时超时限制。这意味着客户端和服务器之间的连接将在 1 小时后超时。这是不可配置的。

后台运行可以执行超过 1 小时，但如果运行时间超过 1 小时，客户端必须重新连接到服务器（例如通过 `POST /threads/{thread_id}/runs/{run_id}/stream` 加入流）以获取运行的输出。

默认为 `86400`。

## `CORS_ALLOW_ORIGINS`

设置 `CORS_ALLOW_ORIGINS` 以指定允许的来源。
- 允许单个来源的示例：`CORS_ALLOW_ORIGINS=https://example.com`
- 允许多个来源的示例：`CORS_ALLOW_ORIGINS=https://example.com,https://app.example.com`

有关高级 CORS 配置，请参阅[如何添加自定义 CORS 配置](/langsmith/cli#customizing-http-middleware-and-headers)。

默认为 `*`（所有来源）。

## `DD_API_KEY`

指定 `DD_API_KEY`（您的 [Datadog API Key](https://docs.datadoghq.com/account_management/api-app-keys/)）以自动为部署启用 Datadog 追踪。指定其他 [`DD_*` 环境变量](https://ddtrace.readthedocs.io/en/stable/configuration.html) 来配置追踪检测。

如果指定了 `DD_API_KEY`，应用程序进程将被包装在 [`ddtrace-run` 命令](https://ddtrace.readthedocs.io/en/stable/installation_quickstart.html) 中。其他 `DD_*` 环境变量（例如 `DD_SITE`、`DD_ENV`、`DD_SERVICE`、`DD_TRACE_ENABLED`）通常需要正确配置追踪检测。请参阅 [`DD_*` 环境变量](https://ddtrace.readthedocs.io/en/stable/configuration.html) 了解更多详情。您可以启用 `DD_TRACE_DEBUG=true` 并设置 `DD_LOG_LEVEL=debug` 进行故障排除。

<Note>
启用 `DD_API_KEY`（以及 `ddtrace-run`）可能会覆盖或干扰您可能已集成到应用程序代码中的其他自动检测解决方案（如 OpenTelemetry）。
</Note>


## `LANGGRAPH_POSTGRES_POOL_MAX_SIZE`

从 langgraph-api 版本 `0.2.12` 开始，可以使用 `LANGGRAPH_POSTGRES_POOL_MAX_SIZE` 环境变量控制 Postgres 连接池的最大大小（每个副本）。通过设置此变量，您可以确定服务器将与 Postgres 数据库建立的并发连接数的上限。

例如，如果部署扩展到 10 个副本且 `LANGGRAPH_POSTGRES_POOL_MAX_SIZE` 配置为 `150`，则最多可建立 1500 个到 Postgres 的连接。这对于数据库资源有限（或更充足）或您需要调整连接行为以提高性能或扩展原因的部署特别有用。

当 [`BG_JOB_ISOLATED_LOOPS`](#bg_job_isolated_loops) 被启用时，池不共享。相反，每个后台 worker 线程创建自己的池，最大大小为 `LANGGRAPH_POSTGRES_POOL_MAX_SIZE / N_JOBS_PER_WORKER`。在降低池大小时请记住这一点。对于共享池效果良好的值可能在隔离循环下导致非常小的每 worker 池。

默认为 `150` 个连接。

## `LS_CHECKPOINT_DELETE`

延迟检查点删除的 JSON 值配置。启用后，线程删除和修剪操作将检查点加入后台删除队列，而不是同步删除，将 I/O 移出请求热路径。在 `langgraph-api>=0.8.1` 中可用。

<Note>
仅支持默认的 PostgreSQL 检查点后端。延迟删除将在未来版本中成为默认设置。
</Note>

接受的字段：

- `enabled`（布尔值，默认 `false`）：当为 `true` 时，线程删除和修剪操作将检查点加入 `checkpoint_delete_queue` 并立即返回，后台 worker 排出队列。
- `enabledWorkerOnly`（布尔值，默认 `false`）：仅运行后台排出 worker 而不加入新条目。在将 `enabled` 回滚为 `false` 后使用此功能来完成队列排出。
- `pollIntervalMs`（整数，默认 `5000`）：worker 轮询队列的频率（毫秒）。
- `batchSize`（整数，默认 `25`）：worker 每次事务排出的检查点条目数。较小的值在更多时间上分散 I/O，但代价是更长的排出延迟。
- `batchSleepMs`（整数，默认 `500`）：当队列非空时，worker 在批次之间睡眠的时间（毫秒）。

示例：`LS_CHECKPOINT_DELETE='{"enabled":true,"batchSize":10,"pollIntervalMs":1000}'`。

默认为禁用（同步检查点删除）。

## `LS_DEFAULT_CHECKPOINTER_BACKEND`

为未在 `langgraph.json` 中指定检查点的 Agent Server 设置默认的[检查点后端](/langsmith/configure-checkpointer)。可接受的值：`"default"`（PostgreSQL）、`"mongo"`、`"custom"`。

如果应用程序的 `langgraph.json` 包含 `checkpointer.backend` 值，则它优先于此变量。

当设置为 `"mongo"` 时，您还必须通过 [`LS_MONGODB_URI`](#ls_mongodb_uri) 提供 MongoDB 连接 URI。

## `LANGSMITH_API_KEY`

仅适用于使用[自托管 LangSmith](/langsmith/self-hosted) 的部署。

要将追踪发送到自托管 LangSmith 实例，请将 `LANGSMITH_API_KEY` 设置为从自托管实例创建的 API 密钥。

## `LANGSMITH_ENDPOINT`

仅适用于使用[自托管 LangSmith](/langsmith/self-hosted) 的部署。

要将追踪发送到自托管 LangSmith 实例，请将 `LANGSMITH_ENDPOINT` 设置为自托管实例的主机名。

## `LANGSMITH_TRACING`

将 `LANGSMITH_TRACING` 设置为 `false` 以禁用追踪到 LangSmith。

<Note>
有关基于运行时条件（如每个客户端要求或数据敏感性）的选择性追踪控制，请参阅[条件追踪](/langsmith/conditional-tracing)。
</Note>

默认为 `true`。

## `LOG_COLOR`

这主要在与通过 `langgraph dev` 命令使用开发服务器相关。将 `LOG_COLOR` 设置为 `true` 以在使用默认控制台渲染器时启用 ANSI 彩色控制台输出。将此变量设置为 `false` 禁用彩色输出会产生单色日志。默认为 `true`。

## `LOG_LEVEL`

配置[日志级别](https://docs.python.org/3/library/logging.html#logging-levels)。默认为 `INFO`。

## `LOG_JSON`

将 `LOG_JSON` 设置为 `true` 以使用配置的 `JSONRenderer` 将所有日志消息呈现为 JSON 对象。这会产生结构化日志，可以轻松解析或摄取到日志管理系统中。默认为 `false`。


## `MOUNT_PREFIX`

<Info>
**仅允许在自托管部署中**
`MOUNT_PREFIX` 环境变量仅允许在自托管部署模型中，LangSmith SaaS 不允许此环境变量。
</Info>

设置 `MOUNT_PREFIX` 以在特定路径前缀下提供 Agent Server。这对于服务器在需要特定路径前缀的反向代理或负载均衡器后面的部署很有用。

例如，如果服务器要在 `https://example.com/langgraph` 下提供，请将 `MOUNT_PREFIX` 设置为 `/langgraph`。

## `N_JOBS_PER_WORKER`

Agent Server 任务队列的每个 worker 的作业数。默认为 `10`。

## `LS_APM_OTEL_ENABLED`

要为您的部署配置 OpenTelemetry APM 追踪，请将 `LS_APM_OTEL_ENABLED` 设置为 `true`，并将 `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` 或 `OTEL_EXPORTER_OTLP_ENDPOINT` 设置为目标追踪摄取端点。请注意，在服务器版本晚于 `0.7.17` 时，需要 `LS_APM_OTEL_ENABLED` 和其他两个导出端点中的一个才能激活 OpenTelemetry APM 追踪。

指定其他 [`OTEL_*` 环境变量](https://opentelemetry.io/docs/collector/configuration/) 来配置追踪、日志和其他检测。

```shell
# If you set LS_APM_OTEL_ENABLED AND (OTEL_EXPORTER_OTLP_TRACES_ENDPOINT or OTEL_EXPORTER_OTLP_ENDPOINT),
# the server starts with OpenTelemetry instrumentation enabled.
LS_APM_OTEL_ENABLED=true
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=<target trace ingestion endpoint>
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp.nr-data.net
OTEL_SERVICE_NAME=MY_LANGSMITH_DEPLOYMENT
OTEL_EXPORTER_OTLP_HEADERS=api-key=<YOUR_INGEST_LICENSE_KEY>
LANGSMITH_OTEL_ENABLED=true
# Common OTEL settings
OTEL_ATTRIBUTE_VALUE_LENGTH_LIMIT=4095
OTEL_EXPORTER_OTLP_COMPRESSION=gzip
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=delta
OTEL_PYTHON_EXCLUDED_URLS=/metrics,/ok,/info
# Optional: OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=true
```

例如，要向 [New Relic 的美国区域](https://docs.newrelic.com/docs/opentelemetry/best-practices/opentelemetry-otlp/) 提交 OpenTelemetry 追踪，请设置以下内容：

```shell
LS_APM_OTEL_ENABLED=true
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=https://otlp.nr-data.net/v1/traces
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp.nr-data.net
OTEL_EXPORTER_OTLP_HEADERS=api-key=<YOUR_INGEST_LICENSE_KEY>
```

<Note>
OTel APM 追踪在 Agent Server 版本 `0.5.32` 中添加，目前处于 Alpha 阶段。
</Note>

## `LS_MONGODB_URI`

MongoDB 检查点后端的 MongoDB 连接 URI。

URI 必须指向副本集成员或 `mongos` 路由器，并且必须在路径中包含数据库名称。

请参阅[配置检查点后端](/langsmith/configure-checkpointer)了解更多详情。

请参阅[配置检查点后端](/langsmith/configure-checkpointer)了解更多详情。

## `POSTGRES_URI_CUSTOM`

<Info>
**仅用于混合和自托管**
自定义 Postgres 实例仅适用于[混合](/langsmith/hybrid)和[自托管](/langsmith/self-hosted)部署。
</Info>

指定 `POSTGRES_URI_CUSTOM` 以使用自定义 Postgres 实例。`POSTGRES_URI_CUSTOM` 的值必须是有效的 [Postgres 连接 URI](https://www.postgresql.org/docs/current/libpq-connect.html#LIBPQ-CONNSTRING-URIS)。

Postgres:

* 版本 15.8 或更高。
* 必须存在一个初始数据库，连接 URI 必须在路径中引用该数据库。

控制平面功能：

* 如果指定了 `POSTGRES_URI_CUSTOM`，控制平面不会为服务器配置数据库。
* 如果移除 `POSTGRES_URI_CUSTOM`，控制平面不会为服务器配置数据库，也不会删除外部管理的 Postgres 实例。
* 如果移除 `POSTGRES_URI_CUSTOM`，部署修订将不会成功。一旦指定了 `POSTGRES_URI_CUSTOM`，它必须在部署的整个生命周期中始终设置。
* 如果删除部署，控制平面不会删除外部管理的 Postgres 实例。
* `POSTGRES_URI_CUSTOM` 的值可以更新。例如，可以更新 URI 中的密码。

数据库连接：

* 自托管 Postgres 实例必须可由 Agent Server 访问。用户负责确保连接。

## `REDIS_CLUSTER`
<Warning>
此功能处于 Alpha 阶段。
</Warning>
<Info>
**仅允许在自托管部署中**
Redis Cluster 模式仅在自托管部署模型中可用，LangSmith SaaS 默认会为您配置一个 redis 实例。
</Info>

将 `REDIS_CLUSTER` 设置为 `True` 以启用 Redis Cluster 模式。启用后，系统将使用集群模式连接到 Redis。在连接到 Redis Cluster 部署时，这很有用。

默认为 `False`。

## `REDIS_KEY_PREFIX`

<Info>
**API Server 版本 0.1.9+ 可用**
此环境变量在 API Server 版本 0.1.9 及以上版本中受支持。
</Info>

指定 Redis 键的前缀。这允许多个 Agent Server 实例通过使用不同的键前缀共享同一个 Redis 实例。

默认为 `''`。

## `REDIS_URI_CUSTOM`

<Info>
**仅用于混合和自托管**
自定义 Redis 实例仅适用于[混合](/langsmith/hybrid)和[自托管](/langsmith/self-hosted)部署。
</Info>

指定 `REDIS_URI_CUSTOM` 以使用自定义 Redis 实例。`REDIS_URI_CUSTOM` 的值必须是有效的 [Redis 连接 URI](https://redis-py.readthedocs.io/en/stable/connections.html#redis.Redis.from_url)。

## `REDIS_MAX_CONNECTIONS`

可以使用 `REDIS_MAX_CONNECTIONS` 环境变量控制 Redis 连接池的最大大小（每个副本）。通过设置此变量，您可以确定服务器将与 Redis 实例建立的并发连接数的上限。

例如，如果部署扩展到 10 个副本且 `REDIS_MAX_CONNECTIONS` 配置为 `150`，则最多可建立 1500 个到 Redis 的连接。

默认为 `2000`。

## `RESUMABLE_STREAM_TTL_SECONDS`

Redis 中可恢复流数据的生存时间（秒）。

当创建运行并流式传输输出时，可以将流配置为可恢复的（例如 `stream_resumable=True`）。如果流是可恢复的，则流的输出会临时存储在 Redis 中。可以通过设置 `RESUMABLE_STREAM_TTL_SECONDS` 来配置此数据的 TTL。

请参阅 [Python](https://reference.langchain.com/python/langsmith/deployment/sdk/#langgraph_sdk.client.RunsClient.stream) 和 [JS/TS](https://langchain-ai.github.io/langgraphjs/reference/classes/sdk_client.RunsClient.html#stream) SDK 了解更多关于如何实现可恢复流的详细信息。

默认为 `120` 秒。

<Note>
为 `RESUMABLE_STREAM_TTL_SECONDS` 设置非常高的值可能导致在有许多具有大或频繁流式输出的并发运行时大量使用 Redis 内存。将此值设置为最小值以在网络中断期间启用恢复，并优先使用检查点以获得长期耐久性和执行快照。
</Note>

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith\env-var.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
