---
title: 如何在一个评估器中返回多个分数
sidebarTitle: 在一个评估器中返回多个分数
---

有时候，自定义评估器或摘要评估器返回多个指标会很有用。例如，如果您有多个由 LLM judge 生成的指标，您可以通过进行一次生成多个指标的 LLM 调用来节省时间和金钱，而不是进行多次 LLM 调用。

要使用 Python SDK 返回多个分数，只需返回以下形式的字典/对象列表：

```python
[
    # 'key' is the metric name
    # 'score' is the value of a numerical metric
    {"key": string, "score": number},
    # 'value' is the value of a categorical metric
    {"key": string, "value": string},
    ... # You may log as many as you wish
]
```

要使用 JS/TS SDK 这样做，请返回一个带有 'results' 键的对象，然后是上述形式的列表

```typescript
{results: [{ key: string, score: number }, ...]};
```

每个字典都可以包含任何或全部的[反馈字段](/langsmith/feedback-data-format)；请查看链接的文档获取更多信息。

示例：

- Python：需要 `langsmith>=0.2.0`
- TypeScript：多分数支持可在 `langsmith@0.1.32` 及更高版本中使用

<CodeGroup>

```python Python
def multiple_scores(outputs: dict, reference_outputs: dict) -> list[dict]:
    # Replace with real evaluation logic.
    precision = 0.8
    recall = 0.9
    f1 = 0.85
    return [
        {"key": "precision", "score": precision},
        {"key": "recall", "score": recall},
        {"key": "f1", "score": f1},
    ]
```

```typescript TypeScript
import type { Run, Example } from "langsmith/schemas";

function multipleScores(rootRun: Run, example: Example) {
  // Your evaluation logic here
  return {
      results: [
          { key: "precision", score: 0.8 },
          { key: "recall", score: 0.9 },
          { key: "f1", score: 0.85 },
      ],
  };
}
```

</CodeGroup>

实验结果的行将显示每个分数。

![multiple_scores.png](/langsmith/images/multiple-scores.png)

## 相关

* [返回分类指标与数值指标](/langsmith/metric-type)

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith\multiple-scores.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
