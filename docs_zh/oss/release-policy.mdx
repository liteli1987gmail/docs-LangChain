---
title: 发布政策
---

本文档介绍 LangChain 和 LangGraph 的发布政策。点击下方标签页查看各自的发布政策：

<Tabs>
    <Tab title="LangChain">

    :::python
    LangChain 生态系统由不同的组件包组成（例如 `langchain-core`、`langchain`、`langchain-community`、合作伙伴包等）
    :::
    :::js
    LangChain 生态系统由不同的组件包组成（例如 `@langchain/core`、`langchain`、`@langchain/community`、合作伙伴包等）
    :::

    ## 发布节奏

    :::python
    随着 LangChain 1.0 的发布，`langchain` 和 `langchain-core` 的**次要**版本（例如从 `1.0.x` 到 `1.1.0`）遵循语义化版本控制，可能频繁发布。次要版本包含新功能和改进，但不包含破坏性更改。
    :::
    :::js
    随着 LangChain 1.0 的发布，`langchain` 和 `@langchain/core` 的**次要**版本（例如从 `1.0.x` 到 `1.1.0`）遵循语义化版本控制，可能频繁发布。次要版本包含新功能和改进，但不包含破坏性更改。
    :::

    补丁版本发布频繁，每周可达数次，因为它们包含错误修复和小的改进。

    ## API 稳定性

    :::python
    LLM 应用程序的开发是一个快速发展的领域，我们不断从用户和社区中学习。因此，我们预期 `langchain` 和 `langchain-core` 中的 API 将继续发展，以更好地满足用户的需求。
    :::
    :::js
    LLM 应用程序的开发是一个快速发展的领域，我们不断从用户和社区中学习。因此，我们预期 `langchain` 和 `@langchain/core` 中的 API 将继续发展，以更好地满足用户的需求。
    :::

    随着 LangChain 1.0 采用语义化版本控制：

    - 公共 API 的破坏性更改仅在主版本发布时发生（例如 `2.0.0`）
    - 次要版本升级（例如 `1.0.0` 到 `1.1.0`）添加新功能而不破坏兼容性
    - 补丁版本升级（例如 `1.0.0` 到 `1.0.1`）包含错误修复和小的改进

    我们通常会尽量避免不必要的更改，并将为被移除的功能提供弃用政策。

    ### 其他包的稳定性

    LangChain 生态系统中其他包的稳定性可能有所不同：

    - **由 LangChain 维护的合作伙伴包**（如 `langchain-openai` 和 `langchain-anthropic`）遵循语义化版本控制，预计在 1.0 之后会稳定。其他合作伙伴包可能遵循不同的稳定性和版本控制政策，用户应参阅这些包的文档获取更多信息。

    :::python
    - **`langchain-community`** 是一个由社区维护的包，包含第三方集成。由于集成数量众多，`langchain-community` 不遵循与 `langchain` 和 `langchain-core` 相同的严格语义化版本控制政策。更多详细信息请参阅长期支持中的"特殊考虑"部分。
    :::
    :::js
    - **`@langchain/community`** 是一个由社区维护的包，包含第三方集成。由于社区贡献和第三方依赖的性质，`@langchain/community` 不遵循与 `langchain` 和 `@langchain/core` 相同的严格语义化版本控制政策。更多详细信息请参阅长期支持中的"特殊考虑"部分。
    :::

    ## 弃用政策

    我们通常会避免弃用功能，直到有更好的替代方案可用。

    :::python
    通过 LangChain 1.0 的语义化版本控制方法，弃用的功能将在整个 1.x 发布系列中继续工作。破坏性更改（包括移除弃用功能）仅在主版本发布时发生（例如 2.0）。

    当 `langchain` 或 `langchain-core` 中的功能被弃用时，我们会：
    - 在代码和文档中明确标记为已弃用
    - 提供迁移指南以推荐替代方案
    - 在整个 1.x 次要版本中为弃用功能提供安全更新
    :::
    :::js
    通过 LangChain 1.0 的语义化版本控制方法，弃用的功能将在整个 1.x 发布系列中继续工作。破坏性更改（包括移除弃用功能）仅在主版本发布时发生（例如 2.0）。

    当 `langchain` 或 `@langchain/core` 中的功能被弃用时，我们会：
    - 在代码和文档中明确标记为已弃用
    - 提供迁移指南以推荐替代方案
    - 在整个 1.x 次要版本中为弃用功能提供安全更新
    :::

    在某些情况下，如果弃用功能没有造成维护问题，我们可能会允许它们在代码库中保留更长时间，以进一步减轻用户的负担。

    ## 长期支持（LTS）

    LangChain 遵循长期支持（LTS）政策，为生产应用程序提供稳定性：

    ### 发布状态定义

    包的标记状态如下之一：
    - **ACTIVE**：当前活跃开发，包括错误修复、安全补丁和新功能
    - **MAINTENANCE**：接收所有安全补丁和关键错误修复，但不包含新功能

    ### 当前的 LTS 发布

    **LangChain 1.0** 被指定为 LTS 发布：
    - **状态**：在 2.0 发布之前保持 ACTIVE
    - **支持期限**：2.0 发布后，1.0 将进入 MAINTENANCE 模式至少 1 年
    - **语义版本合规**：用户可以在次要版本之间升级（例如 1.0 到 1.1）而不会破坏兼容性

    ### 旧版本支持

    **LangChain 0.3**：

    - **状态**：MAINTENANCE 模式
    - **支持期限**：直至 2026 年 12 月
    - **支持内容**：安全补丁和关键错误修复

    ### 特殊考虑

    :::python
    **langchain-community 0.4**：由于社区贡献和第三方集成的性质，`langchain-community` 在次要版本中可能有破坏性更改。它已作为 0.4 版本发布，以反映这种不同的稳定性政策。
    :::
    :::js
    **@langchain/community 1.0**：由于社区贡献和第三方集成的性质，`@langchain/community` 在次要版本中可能有破坏性更改。
    :::

    </Tab>
    <Tab title="LangGraph">
    LangGraph 遵循结构化的发布政策，以确保为构建生产应用程序的用户提供稳定性和可预测性。

    ## 发布节奏

    我们预计**主**版本之间至少相隔 6-12 个月，以，为生产应用程序提供稳定性。

    **次要**版本通常每 1-2 个月发布一次，包含新功能和改进。

    **补丁**版本按需发布，通常每周一次，以解决错误和安全问题。

    ## API 稳定性

    ### 稳定 API

    所有没有特殊前缀的 API 都被视为稳定的，可用于生产。我们维护次要版本内的向后兼容性。

    ### Beta 功能

    文档中标记为 `beta` 的功能：

    - 功能完整且经过测试
    - 可安全用于生产，但需理解它们可能会发生变化
    - 根据用户反馈可能进行次要 API 调整

    ### 实验性功能

    标记为 `experimental` 或 `alpha` 的功能：

    - 正在积极开发中
    - 可能会发生重大变化或被移除
    - 在生产环境中使用时应谨慎

    ### 内部 API

    带有下划线（`_`）前缀或明确标记为内部使用的 API：

    - 不属于公共 API 的一部分
    - 可能随时更改，恕不另行通知
    - 不应直接使用

    ## 弃用政策

    弃用功能时：

    1. **弃用通知**：功能被标记为已弃用，并提供清晰的迁移指南
    2. **宽限期**：弃用功能在至少一个次要版本中保持功能
    3. **移除**：功能仅在主版本发布时移除
    4. **迁移支持**：我们提供迁移指南，并在可行时提供自动化工具

    ## 平台兼容性

    :::python
    ### Python 支持

    - 我们支持由 Python 软件基金会积极维护的 Python 版本
    - Python 版本要求仅在主版本中更改
    - 当前需要 Python 3.10 或更高版本
    :::

    :::js
    ### JavaScript/TypeScript 支持

    - 我们支持 Node.js LTS 版本
    - 为所有公共 API 提供 TypeScript 类型定义
    - 客户端组件的浏览器兼容性已记录
    :::

    ## 破坏性更改

    破坏性更改仅在主版本中引入，包括：

    - 移除已弃用的 API
    - 更改必需参数
    - 影响现有应用程序的默认行为更改
    - 最低 Python/Node.js 版本更新

    ## 迁移支持

    对于主版本升级，我们提供：

    - 全面的迁移指南
    - 可行时的自动化迁移脚本
    - 之前主版本的扩展支持期限
    - 所有破坏性更改的清晰文档

    ## 长期支持（LTS）

    LangGraph 遵循长期支持（LTS）政策，为生产应用程序提供稳定性：

    ### 发布状态定义

    包的标记状态如下：

    - **ACTIVE**：当前活跃开发，包括错误修复、安全补丁和新功能
    - **MAINTENANCE**：接收所有安全补丁和关键错误修复，但不包含新功能

    ### 当前的 LTS 发布

    **LangGraph 1.0** 被指定为 LTS 发布：

    - **状态**：在 2.0 发布之前保持 ACTIVE
    - **支持期限**：2.0 发布后，1.0 将进入 MAINTENANCE 模式至少 1 年
    - **语义版本合规**：用户可以在次要版本之间升级（例如 1.0 到 1.1）而不会破坏兼容性

    ### 旧版本支持

    **LangGraph 0.4**：

    - **状态**：MAINTENANCE 模式
    - **支持期限**：直至 2026 年 12 月
    - **支持内容**：所有安全补丁和关键错误修复

    ## 另请参阅

    - [版本控制](/oss/versioning) - 版本编号和支持详情
    - [发布](/oss/releases) - 版本特定的发布说明和迁移指南
    </Tab>
    <Tab title="Deep Agents">
    :::python
    Deep Agents（`deepagents`）是一个用于构建高级代理架构的快速发展的包。

    ## 1.0 前的状态

    Deep Agents 目前处于 1.0 前的开发状态。虽然我们尽可能减少破坏性更改，但 API 可能随着从社区中学习和适应代理应用程序的快速变化格局而发展。

    ## 发布节奏

    **次要**版本（例如 `0.1.0` 到 `0.2.0`）可能包含新功能，以及随着包成熟而可能出现的破坏性更改。

    **补丁**版本（例如 `0.1.0` 到 `0.1.1`）包含错误修复和小的改进，不会有破坏性更改。

    ## API 稳定性

    作为一个 1.0 前的包：

    - API 可能在次要版本之间更改
    - 我们旨在通过在更改发生时提供清晰的迁移指南来减少干扰
    - 标记为 `experimental` 或 `alpha` 的功能可能会有更大的变化

    ## 弃用政策

    在 Deep Agents 中弃用功能时：

    - 我们会在可能的情况下提前提供弃用警告
    - 迁移指南包含在发布说明中
    - 弃用功能在后续次要版本中移除

    ## 1.0 之路

    Deep Agents 将在以下情况下达到 1.0：

    - 基于社区反馈，核心 API 已稳定
    - 该包已在生产环境中经过实战测试
    - 核心功能不再有破坏性更改

    在 1.0 之后，Deep Agents 将遵循与 LangChain 和 LangGraph 相同的语义化版本控制和 LTS 政策。

    ## 另请参阅

    - [版本控制](/oss/versioning) - 版本编号和支持详情
    :::
    </Tab>

</Tabs>
