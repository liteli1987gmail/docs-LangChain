---
title: "AI21 集成"
description: "Integrate with the AI21 LLM using LangChain JavaScript."
---

You can get started with AI21Labs' Jurassic family of models, as well as see a full list of available foundational models, by signing up for an API key [on their website](https://www.ai21.com/).

以下是 an 示例 of initializing an instance in LangChain.js:

<Tip>
请参阅 [this section for general instructions on installing LangChain 包s](/oss/langchain/install).
</Tip>

```bash npm
npm install @langchain/community @langchain/core
```

```typescript
import { AI21 } from "@langchain/community/llms/ai21";

const model = new AI21({
  ai21ApiKey: "YOUR_AI21_API_KEY", // Or set as process.env.AI21_API_KEY
});

const res = await model.invoke(`Translate "I love programming" into German.`);

console.log({ res });

/*
  {
    res: "\nIch liebe das Programmieren."
  }
 */
```

## 相关内容

- [Models guide](/oss/langchain/models)
