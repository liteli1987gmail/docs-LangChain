---
title: "ChatGPT plugin 集成"
description: "Integrate with the ChatGPT plugin retriever using LangChain JavaScript."
---

<警告>
This module has been deprecated and is no longer supported. 该 documentation below will not work in versions 0.2.0 or later.
</警告>

This 示例 shows how to use the ChatGPT Retriever Plugin within LangChain.

To set up the ChatGPT Retriever Plugin, follow the [ChatGPT Retrieval Plugin setup instructions](https://github.com/openai/chatgpt-retrieval-plugin).

## 使用方法

```typescript
import { ChatGPTPluginRetriever } from "@langchain/classic/retrievers/remote";

const retriever = new ChatGPTPluginRetriever({
  url: "http://0.0.0.0:8000",
  auth: {
    bearer: "super-secret-jwt-token-with-at-least-32-characters-long",
  },
});

const docs = await retriever.invoke("hello world");

console.log(docs);
```

## 相关内容

- [Retrieval guide](/oss/langchain/retrieval)
