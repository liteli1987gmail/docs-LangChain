---
title: "ChatGPT 文件集成"
description: "使用 LangChain JavaScript 与 ChatGPT 文件文档加载器集成。"
---

本示例介绍如何从 ChatGPT 数据导出文件夹加载 conversations.json。您可以通过以下方式获取数据导出：ChatGPT -> (个人资料) - 设置 -> 导出数据 -> 确认导出 -> 查看邮件。

## 使用方法，提取所有日志

示例代码：

```typescript
import { ChatGPTLoader } from "@langchain/community/document_loaders/fs/chatgpt";

const loader = new ChatGPTLoader("./example_data/example_conversations.json");

const docs = await loader.load();

console.log(docs);
```

## 使用方法，提取单个日志

示例代码：

```typescript
import { ChatGPTLoader } from "@langchain/community/document_loaders/fs/chatgpt";

const loader = new ChatGPTLoader(
  "./example_data/example_conversations.json",
  1
);

const docs = await loader.load();

console.log(docs);
```