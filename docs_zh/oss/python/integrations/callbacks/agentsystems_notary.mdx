---
title: AgentSystems Notary
description: Cryptographically verifiable audit trails for LangChain applications.
---

## Overview

AgentSystems Notary 为 AI Agent 交互创建防篡改的审计追踪。

## Why


当人工智能行为受到客户、审计员、监管机构、保险公司等的质疑时，您需要证明实际发生了什么。传统日志不起作用：您控制着它们，因此第三方必须相信您没有修改它们。


防篡改日志消除了这种信任需求。

## How it works

原始 LLM 交互保留在您的存储中。在正常操作期间，第三方无法查看这些数据。但每个交互的加密哈希值会同时写入独立的、防篡改的存储（Arweave 或 AgentSystems 托管服务）中。

如果需要进行审计或出现争议，您可以提供原始日志。审计人员会重新计算哈希值，并与存储的哈希值进行比较。匹配表示日志未被修改。不匹配则表示存在篡改或损坏。

你控制你的数据，但不能改变它而不被检测到。

**记录内容：**

- 记录到您的存储：完整的原始 LLM 数据（prompts、responses、metadata、时间戳）
- 记录到哈希存储：SHA-256 哈希值 + 元数据（例如 namespace、session ID、时间戳）

## Hash storage options

哈希值（不是原始数据）可以被写入任一存储选项：

| Storage | Best For | Features |
|---------|----------|----------|
| [Decentralized (Arweave)](https://docs.agentsystems.ai/notary/configuration/hash-storage?utm_source=langchain-docs&utm_medium=docs) | No vendor lock-in | Public append-only ledger, open-source verification, no account needed |
| [`Custodied`](https://agentsystems.ai/notary?utm_source=langchain-docs&utm_medium=docs) | Managed compliance | Write-once storage, verification UI, signed attestations for audits |

[托管计划](https://agentsystems.ai/notary?utm_source=langchain-docs&utm_medium=docs) 提供符合 WORM 标准的哈希存储、托管签名和签名证明。

## Prerequisites

```shell
pip install agentsystems-notary langchain langchain-anthropic python-dotenv
```

## Example (decentralized)

<步骤>
  <步骤 title="生成签名密钥">
    ```shell
    openssl genrsa -out arweave-key.pem 4096
    ```
<Warning>
保留此密钥。在验证过程中，需要此密钥来证明链上哈希值的所有权。
</Warning>

对于生产环境，请使用[云密钥管理服务](https://docs.agentsystems.ai/notary/configuration/signing?utm_source=langchain-docs&utm_medium=docs)。
  </Step>
  <Step title="创建 .env 文件">
    在项目根目录创建一个 `.env` 文件：
    ```
    # AWS S3 for raw payload storage
    ORG_AWS_S3_BUCKET_NAME=your-bucket
    ORG_AWS_S3_ACCESS_KEY_ID=AKIA...
    ORG_AWS_S3_SECRET_ACCESS_KEY=...
    ORG_AWS_S3_REGION=us-east-1

    # Path to signing key
    ARWEAVE_PRIVATE_KEY_PATH=./arweave-key.pem

    # Anthropic
    ANTHROPIC_API_KEY=sk-ant-...
    ```
运行示例
    ```python
    import os

    from agentsystems_notary import (
        LangChainNotary,
        ArweaveHashStorage,
        AwsS3StorageConfig,
        LocalKeySignerConfig,
        RawPayloadStorage,
    )
    from langchain_anthropic import ChatAnthropic
    from dotenv import load_dotenv

    load_dotenv()

    # Your S3 bucket for raw LLM payloads
    s3_config = AwsS3StorageConfig(
        bucket_name=os.environ["ORG_AWS_S3_BUCKET_NAME"],
        aws_access_key_id=os.environ["ORG_AWS_S3_ACCESS_KEY_ID"],
        aws_secret_access_key=os.environ["ORG_AWS_S3_SECRET_ACCESS_KEY"],
        aws_region=os.environ["ORG_AWS_S3_REGION"],
    )
    raw_payload_storage = RawPayloadStorage(storage=s3_config)

    # Local RSA key for signing
    signer = LocalKeySignerConfig(
        private_key_path=os.environ["ARWEAVE_PRIVATE_KEY_PATH"],
    )

    # Arweave for decentralized hash storage
    # Namespace is public — written to the ledger and used to segment stored data
    # Namespace should be one anonymous ID per customer, agent, or environment
    # Retain a record of your namespace mappings
    arweave_storage = ArweaveHashStorage(
        namespace="tenant_a1b2c3d4",  # See namespace comments above
        signer=signer,
    )

    # Create notary callback
    notary = LangChainNotary(
        raw_payload_storage=raw_payload_storage,
        hash_storage=[arweave_storage],
        debug=True,
    )

    # Attach to model
    model = ChatAnthropic(
        model="claude-sonnet-4-6",
        api_key=os.environ["ANTHROPIC_API_KEY"],
        callbacks=[notary],
    )

    response = model.invoke("What is the capital of France?")
    print(response.content)
    ```
</步骤>

## Verification

**去中心化 (Arweave)**: 从您的存储桶下载原始数据，压缩它们，并使用开源 CLI 进行验证：

```shell
aws s3 sync s3://your-bucket/arweave/tenant_a1b2c3d4/ ./logs
zip -r logs.zip logs
npm install -g agentsystems-verify
agentsystems-verify --logs logs.zip
```

CLI 重新对每个载荷进行哈希运算，并与存储在 Arweave 上的哈希值进行比较。请参阅[完整验证指南](https://docs.agentsystems.ai/notary/verification/arweave?utm_source=langchain-docs&utm_medium=docs)了解更多详情。

另外，[Verify UI](https://verify.agentsystems.ai?utm_source=langchain-docs&utm_medium=docs) 同时支持去中心化验证和托管验证。

## Configuration


- [原始负载存储选项](https://docs.agentsystems.ai/notary/configuration/raw-payload-storage?utm_source=langchain-docs&utm_medium=docs)
- [签名配置](https://docs.agentsystems.ai/notary/configuration/signing?utm_source=langchain-docs&utm_medium=docs)
- [哈希存储选项](https://docs.agentsystems.ai/notary/configuration/hash-storage?utm_source=langchain-docs&utm_medium=docs)

## Resources

- [网站](https://agentsystems.ai/notary?utm_source=langchain-docs&utm_medium=docs)
- [文档](https://docs.agentsystems.ai/notary/?utm_source=langchain-docs&utm_medium=docs)
- [GitHub](https://github.com/agentsystems/agentsystems-notary?utm_source=langchain-docs&utm_medium=docs)
