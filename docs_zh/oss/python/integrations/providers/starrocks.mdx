---
title: "StarRocks integrations"
description: "Integrate with StarRocks using LangChain Python."
---

StarRocks 是一个高性能分析型数据库。
`StarRocks` is a next-gen sub-second MPP database for full analytics scenarios, including multi-dimensional analytics, real-time analytics and ad-hoc query.

通常情况下，`StarRocks` 被归类为 OLAP（在线分析处理）数据库，并且在 [ClickBench — a Benchmark For Analytical DBMS](https://benchmark.clickhouse.com/) 中表现出色。由于它拥有超快的向量化执行引擎，它也可以被用作一个快速的向量数据库（vectordb）。

## Installation and setup


<CodeGroup>
```bash pip
pip install pymysql
```

```bash uv
uv add pymysql
```
</CodeGroup>

## Vector store

See a [usage example](/oss/integrations/vectorstores/starrocks).

```python
from langchain_community.vectorstores import StarRocks
```
