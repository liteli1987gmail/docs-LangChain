---
title: "Chroma integrations"
description: "Integrate with Chroma using LangChain Python."
---

[Chroma](https://docs.trychroma.com/getting-started) 是一个数据库，用于构建带有 embeddings 的 AI 应用。

## Installation and setup

<CodeGroup>
```bash pip
pip install langchain-chroma
```

```bash uv
uv add langchain-chroma
```
</CodeGroup>


## VectorStore

There exists a wrapper around Chroma vector databases, allowing you to use it as a vectorstore,
whether for semantic search or example selection.

```python
from langchain_chroma import Chroma
```

For a more detailed walkthrough of the Chroma wrapper, see [this notebook](/oss/integrations/vectorstores/chroma)

## Retriever

```python
from langchain_classic.retrievers import SelfQueryRetriever
```
