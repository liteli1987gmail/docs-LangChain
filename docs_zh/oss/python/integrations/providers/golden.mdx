---
title: "Golden integrations"
description: "Integrate with Golden using LangChain Python."
---

I see you've provided context about the **Golden Query API** and the `golden-query` LangChain tool. Here's a summary of what you've shared:

## Key Points

| Feature | Description |
|---------|-------------|
| **Purpose** | Natural language queries on the Golden Knowledge Graph |
| **Data Types** | Structured data about entities (companies, products, people, etc.) |
| **LangChain Integration** | `golden-query` tool wraps the Golden Query API |
| **Documentation** | [Golden Query API docs](https://docs.golden.com/reference/query-api) |

## Example Query Types

- `Products from OpenAI`
- `Generative ai companies with series a funding`
- `rappers who invest`

---

**How can I help you with this?** For example:

- Do you want me to write code using the `golden-query` LangChain tool?
- Are you looking for examples of how to integrate it into a LangChain agent?
- Do you have a specific query you want to test or explore?
- Are you comparing this tool with other data sources?

Let me know what you're trying to accomplish! 🏆

## Installation and setup
- Go to the [Golden API docs](https://docs.golden.com/) to get an overview about the Golden API.
- Get your API key from the [Golden API Settings](https://golden.com/settings/api) page.
- Save your API key into GOLDEN_API_KEY env variable

## Wrappers

### Utility

There exists a GoldenQueryAPIWrapper utility which wraps this API. To import this utility:

```python
from langchain_community.utilities.golden_query import GoldenQueryAPIWrapper
```

For a more detailed walkthrough of this wrapper, see [this notebook](/oss/integrations/tools/golden_query).

### Tool

You can also easily load this wrapper as a Tool (to use with an Agent).
You can do this with:
```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["golden-query"])
```

For more information on tools, see [Tools](/oss/integrations/tools).
