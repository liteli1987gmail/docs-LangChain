---
title: "TigerGraph integrations"
description: "Integrate with TigerGraph using LangChain Python."
---

[TigerGraph](https://www.tigergraph.com/tigergraph-db/) 是一个原生分布式的高性能图数据库。
以顶点和边的图格式存储数据能够建立丰富的关系，
非常适合用于 LLM 响应的检索增强（grounding）。

## Installation and setup

Follow instructions [how to connect to the `TigerGraph` database](https://docs.tigergraph.com/pytigergraph/current/getting-started/connection).

Install the Python SDK:

<CodeGroup>
```bash pip
pip install pyTigerGraph
```

```bash uv
uv add pyTigerGraph
```
</CodeGroup>

## Graph store

### TigerGraph

See a [usage example](/oss/integrations/graphs/tigergraph).

```python
from langchain_community.graphs import TigerGraph
```
