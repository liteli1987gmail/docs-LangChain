---
title: LangSmith 共享责任模型
sidebarTitle: 共享责任模型
description: LangChain 与客户如何分担 LangSmith 平台安全责任的概述。
---

LangSmith 作为多租户 SaaS 解决方案运行。我们的安全模型设计得很简单：LangChain 保护平台基础设施和应用程序，而您保护您的特定使用、数据输入以及您构建的 AI 代理。

## 责任矩阵

| 领域 | LangChain 责任（提供商） | 客户责任（用户） |
| :---- | :---- | :---- |
| **基础设施** | 我们管理底层云基础设施（通过 GCP），包括服务器、网络、操作系统补丁和容量规划。GCP 拥有物理数据中心。 | 不适用。您不在 SaaS 环境中配置或维护计算资源。 |
| **应用程序** | 我们保护 LangSmith 应用程序代码、API 端点和数据库集群，包括代码扫描和渗透测试。 | 您负责使用我们的 SDK 构建的 AI 链和代理的安全性和安全性。 |
| **数据** | 我们使用 AES-256 加密静态数据，使用 TLS 1.2 或更高版本加密传输中的数据，并强制执行租户隔离。 | 您控制发送给我们哪些数据，并必须在数据离开您的环境之前通过 SDK 过滤敏感 PII。 |
| **身份** | 我们提供护栏，包括 SSO/SCIM、MFA 强制选项和 RBAC 框架。 | 您管理您的用户列表，分配角色（例如 Admin vs. Viewer），并撤销离职员工的访问权限。 |
| **密钥** | 我们安全地存储您委托给平台的密钥。 | 您负责轮换您的 API 密钥，并确保它们不会硬编码在您的应用程序中。 |

## LangChain 责任（平台）

- 我们维护 SOC 2 Type II、GDPR 和 HIPAA 合规性，并接受年度第三方审计和渗透测试。
- 我们在 Google Cloud Platform (GCP) 上管理所有底层基础设施，包括网络防火墙、通过 Cloud Armor 提供的 DDoS 保护以及容器安全。
- 我们根据 SLA 维护高可用性，维护日常备份，并处理 LangSmith 服务的灾难恢复。
- 我们在严格的服务水平协议内修补已确认的平台漏洞，关键严重性问题在不到 2 周内修复，高严重性问题在 30 天内修复。
- 我们使用 AES-256 加密所有客户数据静态数据，使用 TLS 1.2 或更高版本加密传输中的数据。

## 客户责任（使用）

- 您必须强制执行最小特权访问，并立即移除离职员工的访问权限。
- 您必须确保不向平台发送禁止的数据（如 PCI DSS 卡持有人数据），并使用 SDK 中的屏蔽功能在源头编辑 PII。
- 您负责您运行 LangChain SDK 的环境的安全性，包括您的笔记本电脑和服务器。
- 您必须定期轮换您的 API 密钥，并确保它们存储在环境变量中而不是硬编码在您的源代码中。

## 客户安全最佳实践

为与我们 SOC 2 Type II 框架中的安全假设保持一致，我们建议客户维护以下内部指南：

- 在您的租户设置中保持最新的技术和安全联系方式，以便我们的团队在事件期间能够联系到您。
- 如果怀疑密钥泄露，请立即通过自助服务门户轮换密钥。如果您有问题或需要有关违规行为的帮助，您可以随时联系 LangChain 安全团队。
- 为您的特定应用程序制定灾难恢复计划，以处理 LangSmith 服务可能不可用的场景。
- 确保用于访问 LangSmith UI 的工作站和端点定期打补丁和保护。
