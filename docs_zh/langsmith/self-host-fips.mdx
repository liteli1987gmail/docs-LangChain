---
title: FIPS 合规镜像
sidebarTitle: FIPS 合规镜像
description: 在符合 FIPS 140 标准的容器镜像上运行自托管 LangSmith 安装
---

<Note>
FIPS 和气隙 LangSmith 部署需要在推广之前与你的 LangChain 客户经理进行讨论。在开始更改安装之前，请联系以了解许可范围、支持配置和升级路径。
</Note>

从 v15 开始，每个 LangChain 编写的 LangSmith 镜像都有一个在 FIPS 140 模式下运行的 `-fips` 对应版本。当你的自托管部署需要 FIPS 合规性时使用这些镜像，例如联邦机构、国防承包商和受监管行业。

## 镜像构建方式

`-fips` 变体构建在 [Chainguard FIPS 容器镜像](https://edu.chainguard.dev/chainguard/fips/fips-images/) 之上，其中包含 NIST 验证的加密模块（OpenSSL FIPS 提供者、Bouncy Castle FIPS 或 BoringCrypto，取决于基础镜像）。有关模块及其 CMVP 证书的列表，请参阅 [Chainguard 的 FIPS 承诺](https://edu.chainguard.dev/chainguard/fips/fips-images/)。

每个 LangChain 编写的镜像都有一个以与 non-FIPS 版本相同标签发布的 `-fips` 对应版本：

| Non-FIPS 镜像 | FIPS 镜像 |
| --- | --- |
| `langchain/langsmith-ace-backend` | `langchain/langsmith-ace-backend-fips` |
| `langchain/langsmith-backend` | `langchain/langsmith-backend-fips` |
| `langchain/langsmith-frontend` | `langchain/langsmith-frontend-fips` |
| `langchain/langsmith-go-backend` | `langchain/langsmith-go-backend-fips` |
| `langchain/langsmith-playground` | `langchain/langsmith-playground-fips` |
| `langchain/hosted-langserve-backend` | `langchain/hosted-langserve-backend-fips` |
| `langchain/langgraph-operator` | `langchain/langgraph-operator-fips` |

LangChain 不发布 PostgreSQL、Redis 和 ClickHouse 的 FIPS 变体。如果你的部署需要这些组件的 FIPS，请通过 [外部 PostgreSQL](/langsmith/self-host-external-postgres)、[外部 Redis](/langsmith/self-host-external-redis) 或 [外部 ClickHouse](/langsmith/self-host-external-clickhouse) 提供你自己的 FIPS 模式服务并连接。

## 使用 FIPS 镜像

更新 LangSmith Helm 安装中的 `values.yaml`，将每个 LangChain 镜像仓库指向其 `-fips` 对应版本，保持你现有的标签。将 `0.15.0` 替换为你想要部署的 [LangSmith 版本](/langsmith/self-hosted-changelog)：

```yaml
images:
  aceBackendImage:
    repository: "langchain/langsmith-ace-backend-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
  backendImage:
    repository: "langchain/langsmith-backend-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
  frontendImage:
    repository: "langchain/langsmith-frontend-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
  hostBackendImage:
    repository: "langchain/hosted-langserve-backend-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
  operatorImage:
    repository: "langchain/langgraph-operator-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
  platformBackendImage:
    repository: "langchain/langsmith-go-backend-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
  playgroundImage:
    repository: "langchain/langsmith-playground-fips"
    pullPolicy: IfNotPresent
    tag: "0.15.0"
```

应用更改并按照 [升级 LangSmith](/langsmith/self-host-upgrades) 指南进行升级。

## 验证 FIPS 模式

Chainguard 在每个 FIPS 镜像中附带了 `openssl-fips-test` 工具。针对 pod 运行它会打印 FIPS 自测、活动的 FIPS 提供者版本以及适用 CMVP 证书的链接。

检查正在运行的 pod：

```bash
kubectl exec <pod-name> -- openssl-fips-test
```

预期输出（节选）：

```text
Checking OpenSSL lifecycle assurance.
	✓ Self-test KAT_Integrity HMAC ... passed.
	✓ Self-test Module_Integrity HMAC ... passed.
	...
	✓ 29 out of 29 self-tests passed.
	✓ Check FIPS cryptographic module is available... passed.
	✓ Check FIPS approved only mode (EVP_default_properties_is_fips_enabled)... passed.
Public OpenSSL API (libssl.so & libcrypto.so):
	name:      OpenSSL 3.6.0 1 Oct 2025
	version:   3.6.0
FIPS cryptographic module provider details (fips.so):
	name:      OpenSSL FIPS Provider
	version:   3.1.2
Locate applicable CMVP certificate(s) at: CMVP #4985
```

你也可以在 Kubernetes 外部验证镜像：

```bash
docker run --rm --entrypoint openssl-fips-test langchain/langsmith-go-backend-fips:0.15.0
```

有关解释输出的更多详细信息，请参阅 [Chainguard 的 FIPS 验证指南](https://edu.chainguard.dev/chainguard/fips/verify-fips/)。

## 气隙部署的镜像复制

镜像复制到私有注册表时，`-fips` 命名约定同样适用。按照 [镜像复制指南](/langsmith/self-host-mirroring-images) 操作，并将每个仓库替换为其 `-fips` 对应版本。无论是否有 FIPS，气隙推广都需要在开始之前与你的 LangChain 客户经理进行讨论。