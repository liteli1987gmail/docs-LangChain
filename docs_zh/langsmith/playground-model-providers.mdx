---
title: 模型提供商
sidebarTitle: 模型提供商
---

[Playground](/langsmith/prompt-engineering-concepts#playground) 支持广泛的模型提供商。您可以选择一个提供商、配置您的首选设置，并保存这些配置以在多个提示词中重复使用。

使用此页面获取可用提供商及其配置选项的列表：

<div className="grid grid-cols-1 md:grid-cols-3 gap-3">
    <a href="#amazon-bedrock" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/bedrock.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/bedrock.svg" alt="" />
        <span className="font-semibold">Amazon Bedrock</span>
    </a>

    <a href="#anthropic" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/anthropic.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/anthropic.svg" alt="" />
        <span className="font-semibold">Anthropic</span>
    </a>

    <a href="#azure-openai" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/microsoft.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/microsoft.svg" alt="" />
        <span className="font-semibold">Azure OpenAI</span>
    </a>

    <a href="#deepseek" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/deepseek.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/deepseek.svg" alt="" />
        <span className="font-semibold">DeepSeek</span>
    </a>

    <a href="#fireworks" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/fireworks.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/fireworks.svg" alt="" />
        <span className="font-semibold">Fireworks</span>
    </a>

    <a href="#google-gemini" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/gemini.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/gemini.svg" alt="" />
        <span className="font-semibold">Google Gemini</span>
    </a>

    <a href="#google-vertex-ai" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/gemini.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/gemini.svg" alt="" />
        <span className="font-semibold">Google Vertex AI</span>
    </a>

    <a href="#groq" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/groq.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/groq.svg" alt="" />
        <span className="font-semibold">Groq</span>
    </a>

    <a href="#mistral-ai" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/mistral.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/mistral.svg" alt="" />
        <span className="font-semibold">Mistral AI</span>
    </a>


    <a href="#openai" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/openai.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/openai.svg" alt="" />
        <span className="font-semibold">OpenAI</span>
    </a>

    <a href="#openai-compatible-endpoint" className="flex items-center justify-center gap-2 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <Icon icon="link" iconType="solid" className="w-5 h-5 flex-shrink-0 mx-1" />
        <span className="font-semibold">OpenAI compatible endpoint</span>
    </a>

    <a href="#xai" className="flex items-center justify-center gap-1.5 p-2 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600 no-underline">
        <img className="block dark:hidden w-5 h-5" src="/images/providers/light/xai.svg" alt="" />
        <img className="hidden dark:block w-5 h-5" src="/images/providers/dark/xai.svg" alt="" />
        <span className="font-semibold">XAI</span>
    </a>

</div>

有关创建和管理模型配置的详细信息，请参阅[配置提示词设置](/langsmith/managing-model-configurations)页面。

## Amazon Bedrock

在使用此模型之前，请确保您有 [AWS 凭证或 IAM 角色](https://docs.aws.amazon.com/bedrock/latest/userguide/security-iam.html)。

### 身份验证

Amazon Bedrock 支持两种身份验证方法。**IAM 信任实体是推荐的方法**，因为它避免了与 LangSmith 共享长期 AWS 访问密钥。

#### IAM 信任实体（推荐）

使用 IAM 信任实体身份验证，您可以在您的 AWS 账户中创建一个 IAM 角色，并允许 LangSmith 担任它。LangSmith 中不存储访问密钥。相反，LangSmith 在每次请求时使用 [AWS STS](https://docs.aws.amazon.com/STS/latest/APIReference/welcome.html) 来担任角色。

设置步骤：

1. 在您的 AWS 账户中创建一个具有调用 Bedrock 模型权限的 IAM 角色（例如 `bedrock:InvokeModel`）。
2. 添加一个信任策略，允许 LangSmith 的 AWS 账户 (`808407022534`) 担任该角色，使用您的 LangSmith 工作区 ID 作为外部 ID：

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::808407022534:root"
      },
      "Action": "sts:AssumeRole",
      "Condition": {
        "StringEquals": {
          "sts:ExternalId": "<your-langsmith-workspace-id>"
        }
      }
    }
  ]
}
```

<Tip>
您可以在您的 [LangSmith 工作区设置](https://smith.langchain.com/settings) 中找到您的工作区 ID。
</Tip>

3. 在 LangSmith Playground 中，展开 Bedrock 提供商下的 **IAM Trusted Entity** 部分，并输入您创建的角色的 ARN。

有关信任策略的更多详细信息，请参阅 [AWS 文档](https://aws.amazon.com/blogs/security/how-to-use-trust-policies-with-iam-roles/)。

#### 访问密钥

或者，您可以使用 AWS 访问密钥（`AWS_ACCESS_KEY_ID` 和 `AWS_SECRET_ACCESS_KEY`）进行身份验证。在 Playground 的 Bedrock 提供商配置中输入这些内容。此方法设置更简单，但安全性较低，因为它需要存储长期凭证。

### 可用模型

AWS Bedrock 提供对多个提供商的基础模型的访问：

- **Anthropic：** Claude 模型。
- **Amazon：** Titan 模型。
- **Cohere：** Command 模型。
- **Meta：** Llama 模型。
- **其他：** 基于地区可用的其他提供商。

有关当前可用模型的列表，请参阅 [AWS Bedrock 文档](https://docs.aws.amazon.com/bedrock/latest/userguide/models-supported.html)。

### 配置参数

参数取决于底层模型提供商：

#### 对于 Anthropic 模型
使用 Anthropic 配置（请参阅下面的 [Anthropic](#anthropic) 部分）。

#### 对于 Amazon Titan
| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 1.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |

#### AWS 特定设置

- **Region：** 模型部署的 AWS 区域。

### 工具调用

取决于底层模型：
- **Anthropic 模型：** `auto`, `any`。
- **Cohere 模型：** `auto`。

## Anthropic

在使用此模型之前，请确保您有 [Anthropic API 密钥](https://console.anthropic.com/settings/keys)。

### 可用模型

Anthropic 在其 Claude 系列中提供三个层级的模型：

- **Opus：** 最高智能和能力。
- **Sonnet：** 平衡的性能和成本。
- **Haiku：** 快速且经济高效。

最新的 Claude 模型支持扩展思维功能，用于显示推理过程。

有关当前可用模型的列表，请参阅 [Anthropic 文档](https://docs.anthropic.com/claude/docs/models-overview)。

### 配置参数

| 参数 | 范围 | 默认值 | 描述 |
|-----------|-------|---------|-------------|
| **Temperature** | 0.0 - 1.0 | 可选 | 随机性控制（取消选中以使用模型默认值） |
| **Max Output Tokens** | 1+ | 1024 | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 可选 | 核采样（取消选中以使用模型默认值） |
| **Top K** | 1+ | 可选 | 限制为 Top K 个 token（取消选中以使用模型默认值） |

<Note>
Temperature、Top P 和 Top K 是可选的。取消选中时，Claude 使用其内部默认值。
</Note>

#### 扩展思维

可在支持的 Claude 模型上使用。启用模型以在响应前显示推理，类似于 OpenAI 的 o 系列。

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Enable Extended Thinking** | 切换 | 显示/隐藏思维过程 |
| **Budget Tokens** | 1+ | 思维的最大 token 数（默认：1024） |

启用后，响应包括：

1. 带有模型推理的「thinking」部分。
1. 最终响应。

#### 高级选项

- **Base URL：** 为自定义部署覆盖 API 端点。

### 工具调用

- **支持的工具选择：** `auto`, `any`（至少需要一个工具）。
- **并行执行：** 否（仅顺序）。

## Azure OpenAI

在使用此模型之前，请确保您有 [Azure OpenAI 凭证](https://learn.microsoft.com/en-us/azure/ai-services/openai/quickstart)（端点 + API 密钥）。

### 可用模型

Azure OpenAI 提供与 OpenAI 相同的模型系列：

- **GPT 系列：** 通用聊天模型。
- **o 系列：** 推理优化模型。
- **遗留模型：** GPT-3.5 和 GPT-4 变体。

模型可用性因 Azure 区域而异，需要在使用前部署。

有关当前可用模型的列表，请参阅 [Azure OpenAI 文档](https://learn.microsoft.com/en-us/azure/ai-services/openai/concepts/models)。

### 配置参数

Azure OpenAI 支持与 OpenAI 相同的参数：

#### 标准参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 控制随机性。越低 = 越集中，越高 = 越有创意。 |
| **Max Output Tokens** | 1+ | 响应的最大长度 |
| **Top P** | 0.0 - 1.0 | 核采样阈值。temperature 的替代方案。 |
| **Presence Penalty** | -2.0 - 2.0 | 惩罚新主题（正）或鼓励它们（负） |
| **Frequency Penalty** | -2.0 - 2.0 | 惩罚重复（正）或允许重复（负） |
| **Seed** | 整数 | 用于可重复输出 |

#### 高级参数

**Reasoning Effort：** 可用于推理优化模型（o 系列和更新的 GPT 模型）。

**Service Tier：** 可用于更新的模型。

**其他参数：**
- **JSON Mode：** 强制有效 JSON 响应。
- **Parallel Tool Calls：** 并发执行多个工具。

#### Azure 特定功能

- **部署管理：** 模型必须在使用前部署。
- **区域可用性：** 选择数据驻留的 Azure 区域。
- **内容过滤：** 内置内容审核和安全功能。
- **托管标识：** Azure AD 身份验证支持。
- **私有端点：** VNet 集成以实现安全访问。

### 工具调用

- **支持的工具选择：** `auto`, `required`, `none` 或特定工具名称。
- **并行执行：** 是。

## DeepSeek

在使用此模型之前，请确保您有 [DeepSeek API 密钥](https://platform.deepseek.com/api_keys)。

### 可用模型

DeepSeek 提供通用模型、推理优化模型（R 系列）和编码专用模型。

有关当前可用模型的列表，请参阅 [DeepSeek 的文档](https://platform.deepseek.com/api-docs/)。

### 配置参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |
| **Presence Penalty** | -2.0 - 2.0 |  |
| **Frequency Penalty** | -2.0 - 2.0 |  |

## Fireworks

在使用此模型之前，请确保您有 [Fireworks API 密钥](https://fireworks.ai/api-keys)。

### 可用模型

Fireworks 为流行的开源模型和微调变体提供高速推理，包括：

- **Llama：** Meta 的各种尺寸的 Llama 模型。
- **Mixtral：** Mistral 的专家混合模型。
- **Qwen：** 阿里巴巴的多语言模型。
- **DeepSeek：** DeepSeek 模型。
- **其他开放模型：** Gemma、Phi 等。

有关当前可用模型的列表，请参阅 [Fireworks 的模型文档](https://docs.fireworks.ai/models)。

### 配置参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |

### 工具调用

- **支持的工具选择：** `auto`, `required`, `none`。
- **并行执行：** 是。

## Google Gemini

在使用此模型之前，请确保您有 [Google AI API 密钥](https://aistudio.google.com/app/apikey)。

### 可用模型

Google 提供多种层级的 Gemini 模型（Ultra、Pro、Flash），针对不同用例进行优化。

有关当前可用模型的列表，请参阅 [Google 的 Gemini 文档](https://ai.google.dev/models/gemini)。

### 配置参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Output Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |
| **Top K** | 1+ | Top-k 采样 |

### 工具调用

- **支持的工具选择：** `auto`, `any`, `none`。
- **并行执行：** 否。

## Google Vertex AI

在使用此模型之前，请确保您有 [Google Cloud 凭证](https://cloud.google.com/vertex-ai/docs/authentication)。

### 可用模型

Google 提供多种层级的 Gemini 模型（Ultra、Pro、Flash），针对不同用例进行优化，以及通过 Vertex AI 可用的其他模型。

有关当前可用模型的列表，请参阅 [Vertex AI 文档](https://cloud.google.com/vertex-ai/docs/generative-ai/models)。

### 配置参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Output Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |
| **Top K** | 1+ | Top-k 采样 |

#### 高级选项

- **Region Selection：** 在特定的 Google Cloud 区域中部署。
- **Safety Settings：** 配置内容过滤阈值。

### 工具调用

- **支持的工具选择：** `auto`, `any`, `none`。
- **并行执行：** 否。

## Groq

在使用此模型之前，请确保您有 [Groq API 密钥](https://console.groq.com/keys)。

### 可用模型

Groq 为流行的开源模型提供高速推理，包括 Llama、Mixtral 和 Gemma 变体。

有关当前可用模型的列表，请参阅 [Groq 的模型文档](https://console.groq.com/docs/models)。

### 配置参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |

### 工具调用

- **支持的工具选择：** `auto`, `required`, `none`。
- **并行执行：** 是。

## Mistral AI

在使用此模型之前，请确保您有 [Mistral AI API 密钥](https://console.mistral.ai/api-keys/)。

### 可用模型

Mistral 提供多种层级（Large、Medium、Small）的模型，针对不同性能和成本要求进行优化。

有关当前可用模型的列表，请参阅 [Mistral 的文档](https://docs.mistral.ai/platform/endpoints/)。

### 配置参数

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 1.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |

### 工具调用

- **支持的工具选择：** `auto`, `any`, `none`。
- **并行执行：** 否。

## OpenAI

在使用此模型之前，请确保您有 [OpenAI API 密钥](https://platform.openai.com/api-keys) 或 [Azure OpenAI 凭证](https://learn.microsoft.com/en-us/azure/ai-services/openai/quickstart)。

### 可用模型

OpenAI 提供多种具有不同能力和价格点的模型系列：

- **GPT 系列：** 具有各种尺寸/能力层级的通用聊天模型。
- **o 系列：** 针对复杂问题解决优化的推理聚焦模型。
- **遗留模型：** 较旧的 GPT-3.5 和 GPT-4 变体。

有关当前可用模型的列表，请参阅 [OpenAI 文档](https://platform.openai.com/docs/models)。

### 配置参数

标准：

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 控制随机性。越低 = 越集中，越高 = 越有创意。 |
| **Max Output Tokens** | 1+ | 响应的最大长度 |
| **Top P** | 0.0 - 1.0 | 核采样阈值。temperature 的替代方案。 |
| **Presence Penalty** | -2.0 - 2.0 | 惩罚新主题（正）或鼓励它们（负） |
| **Frequency Penalty** | -2.0 - 2.0 | 惩罚重复（正）或允许重复（负） |
| **Seed** | 整数 | 用于可重复输出 |

高级：

**Reasoning Effort**：可用于推理优化模型（o 系列和更新的 GPT 模型）。

控制响应前的推理深度。更高的 effort = 复杂任务的质量更好，延迟更长。

| 值 | 描述 |
|-------|-------------|
| `none` | 禁用推理（标准聊天行为） |
| `minimal` | 最小推理 |
| `low` | 轻度推理 |
| `medium` | 中度推理（默认） |
| `high` | 深度推理 |
| `xhigh` | 极深度推理（如果模型支持） |

<Note>
当 reasoning_effort 处于活动状态（非 `none`）时，temperature、top_p 和惩罚会自动禁用。
</Note>

**Service Tier**：可用于更新的模型。

控制请求优先级和处理分配。

| 值 | 描述 |
|-------|-------------|
| `auto` | 系统根据负载决定（默认） |
| `default` | 标准处理队列 |
| `flex` | 更低成本，可变延迟（如果模型支持） |
| `priority` | 高优先级队列，更低延迟，更高成本 |

**其他参数：**
- **JSON Mode：** 强制有效 JSON 响应。
- **Responses API：** 改进的流式传输（默认：启用）。
- **Parallel Tool Calls：** 并发执行多个工具。

### 工具调用

- **支持的工具选择：** `auto`, `required`, `none` 或特定工具名称
- **并行执行：** 是

## OpenAI 兼容端点

身份验证因端点而异（通常是 API 密钥或无）。

### 配置

**必需：**
- **Base URL：** 您的端点 URL（例如 `https://your-endpoint.com/v1`）。
- **Model Name：** 您的模型标识符。

适用于任何实现 OpenAI 兼容 API 格式的框架或服务，包括：

- 自托管开源推理服务器
- 模型路由代理
- 自定义模型端点

### 配置参数

所有 OpenAI 兼容参数：

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |
| **Frequency Penalty** | -2.0 - 2.0 | 减少重复 |
| **Presence Penalty** | -2.0 - 2.0 | 鼓励新主题 |

**高级：**
- **JSON Mode：** 如果端点支持。
- **Streaming：** 如果端点支持。
- **Function Calling：** 如果端点实现 OpenAI 格式。

### 工具调用

- **支持的工具选择：** `auto`, `required`, `none`（如果端点支持）。
- **并行执行：** 是（如果端点支持）。

### 示例端点

**本地 Ollama：**
```
Base URL: http://localhost:11434/v1
Model: llama3.1
```

**vLLM Server：**
```
Base URL: https://your-server.com/v1
Model: mistral-7b-instruct
```

**LiteLLM Proxy：**
```
Base URL: https://litellm.example.com
Model: gpt-4 (routes to configured backend)
```

## XAI

在使用此模型之前，请确保您有 [xAI API 密钥](https://console.x.ai/)。

### 可用模型

xAI 提供多种尺寸的 Grok 模型，适用于不同用例。

有关当前可用模型的列表，请参阅 [xAI 的文档](https://docs.x.ai/docs)。

### 配置参数

标准 OpenAI 兼容参数：

| 参数 | 范围 | 描述 |
|-----------|-------|-------------|
| **Temperature** | 0.0 - 2.0 | 响应随机性 |
| **Max Tokens** | 1+ | 最大响应长度 |
| **Top P** | 0.0 - 1.0 | 核采样 |
| **Presence Penalty** | 0 - 2.0 | 在推理模型上隐藏 |
| **Frequency Penalty** | 0 - 2.0 | 在推理模型上隐藏 |

### 工具调用

- **支持的工具选择：** OpenAI 兼容。
- **并行执行：** 是（如果支持）。

## 所有提供商的通用配置

### 额外参数

所有提供商支持**额外参数的 JSON 编辑器**，这些参数在 UI 中未公开：

```json
{
  "logprobs": true,
  "top_logprobs": 5,
  "custom_parameter": "value"
}
```

**用例：**
- 提供商特定的 beta 功能
- 尚未在 UI 中的高级参数
- 用于跟踪的自定义元数据

**限制：** 无法覆盖 UI 中已有的参数（例如，如果在上面设置了 temperature，则无法在此处设置）

### 速率限制

**每秒请求数（RPS）** - 在通过数据集运行时所有提供商可用：

- **范围：** 0 - 500 RPS
- **目的：** 遵守 API 速率限制，控制成本
- **默认值：** 因提供商而异

在运行实验或评估时设置此参数以避免达到速率限制。

## 下一步

<CardGroup cols={2}>
  <Card
    title="配置提示词设置"
    icon="settings"
    href="/langsmith/managing-model-configurations"
  >
    了解如何在 Playground 中创建和管理模型配置。
  </Card>

  <Card
    title="创建提示词"
    icon="edit"
    href="/langsmith/create-a-prompt"
  >
    开始使用您选择的模型提供商构建提示词。
  </Card>
</CardGroup>